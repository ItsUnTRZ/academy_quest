<%= turbo_frame_tag dom_id(quest) do %>
  <div class="flex flex-col sm:flex-row justify-between items-center pb-5 sm:pb-0">
    <div class="w-full sm:w-auto my-5">
      <%= quest.name %>
    </div>
    <div class="w-full sm:w-auto">
      <%= button_to "Destroy", quest, method: :delete, class: "w-full sm:w-auto rounded-md px-3.5 py-2.5 text-white bg-red-600 hover:bg-red-500 font-medium cursor-pointer" %>
    </div>
  </div>
<% end %>
